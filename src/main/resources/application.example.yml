
spring:
  application:
    name: auth
  data:
    mongodb:
      uri: mongodb://username:password@host:port/db?authSource=admin
      auto-index-creation: true

springdoc:
  api-docs: 
    path: /api-docs

jwt:
  secret: 256-bit-key
  refresh: 256-bit-key

server:
  port: 8081

